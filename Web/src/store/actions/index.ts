import * as application from './application';
import * as user from './user';
import * as project from './project';

const StoreAction = {
  application,
  user,
  project,
};

export { StoreAction };
